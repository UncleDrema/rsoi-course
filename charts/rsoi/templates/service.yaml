apiVersion: v1
kind: Service
metadata:
  name: {{ include "rsoi.fullname" . }}
  labels:
    {{- include "rsoi.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: {{ .Values.service.name }}
  selector:
    {{- include "rsoi.selectorLabels" . | nindent 4 }}
